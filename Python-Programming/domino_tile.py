for left in range(7):
    for right in range(left, 7):
        print("[" + str(left) + "|" + str(right) + "]" , end= " ")
        # The above code generates all possible domino tiles with values from 0 to 6.
    print()
    